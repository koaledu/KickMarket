@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="has-text-warning title">Error.</h1>
<h2 class="has-text-warning subtitle">Se produjo un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p class="my-4">
        <strong>ID de la solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3 class="title is-4">Modo de desarrollador</h3>

<p class="my-4">
    Cambiar al entorno a <strong>Development</strong> va a mostrar
    información más detallada del error.
</p>

<p class="my-4">
    <strong>
        El entorno de desarrollo no debe estar habilitado para las aplicaciones
        en producción.
    </strong>
    Puede resultar en la visualización de información confidencial de
    excepciones a los usuarios finales.
    Para la depuración local, habilite el entorno de <strong>Development</strong>
    configurando la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong>
    a <strong>Development</strong> y reiniciando la aplicación.
</p>
